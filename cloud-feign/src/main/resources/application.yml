eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8765
spring:
  application:
    name: cloud-feign

# 在feign中使用断路器
feign:
  hystrix:
    enabled: true



